import {Text, View} from "react-native";

const PowiadomieniaScreen = ({ navigation }) => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>PowiadomieniaScreen</Text>
        </View>
    );
}

export default PowiadomieniaScreen;
