import {Text, View} from "react-native";

const EkranGlownyScreen = ({ navigation }) => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>EkranGłównyScreen</Text>
        </View>
    );
}

export default EkranGlownyScreen;
